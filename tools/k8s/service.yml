kind: Service
apiVersion: v1
metadata:
  name: wallets
spec:
  type: NodePort
  selector:
    app: wallets
  ports:
  - protocol: TCP
    name: management
    port: 8558
    targetPort: management
  - protocol: TCP
    name: api
    port: 8080
    targetPort: api